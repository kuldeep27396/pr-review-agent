[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -m compileall pr_review_agent"]

[start]
cmd = "uvicorn pr_review_agent.main:app --host 0.0.0.0 --port ${PORT:-3000}"
